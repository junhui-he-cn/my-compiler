# Array `any` / `all` Implementation Plan

## Tasks

- [x] Add red success, shadowing, runtime-error, type-error, and artifact fixtures.
- [ ] Register `any` and `all`, add static predicate checking, and add member lowering.
- [ ] Implement snapshot short-circuit callback execution in the Rust VM with unit tests.
- [x] Generate and review AST, IR, bytecode, `.cdbc`, and run outputs.
- [x] Update README, AGENTS, roadmap, and bytecode-format documentation.
- [x] Run the full CMake, golden, artifact, Rust VM, Cargo, and hygiene suite.
