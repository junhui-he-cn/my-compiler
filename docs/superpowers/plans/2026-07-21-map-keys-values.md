# Map `keys` / `values` Implementation Plan

## Tasks

- [ ] Add red success, shadowing, runtime-error, Rust unit, and artifact
  fixtures.
- [ ] Register `keys` and `values`, add static map/result typing, and add
  unshadowed member-call lowering.
- [ ] Implement ordered fresh-array snapshots in the Rust VM.
- [ ] Generate/review AST, IR, bytecode, `.cdbc`, and run outputs.
- [ ] Update README, AGENTS, roadmap, and bytecode-format documentation.
- [ ] Run the full CMake, golden, artifact, Rust VM, Cargo, and hygiene suite.
