# Map `for-in` Implementation Plan

## Tasks

- [ ] Add red map iteration success fixtures and a Rust `AssertArray` snapshot
  test.
- [ ] Extend `TypeChecker` to accept maps, infer the key binding type, and
  update non-iterable diagnostics.
- [ ] Extend Rust `AssertArray` execution to snapshot map keys in insertion
  order while preserving array/range behavior.
- [ ] Add AST/IR/bytecode golden output, `.cdbc` artifact coverage, and dynamic
  runtime coverage.
- [ ] Update `README.md`, `AGENTS.md`, `docs/roadmap.md`, and
  `docs/bytecode-text-format.md`.
- [ ] Run the complete CMake, golden, artifact, Rust VM, Cargo, and hygiene
  verification commands.
