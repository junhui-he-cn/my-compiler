# Array Map Implementation Plan

## Goal

Implement the first higher-order array API through the existing shadowable
native function and unshadowed member-call paths.

## Tasks

- [x] Add success, type-error, runtime-error, and bytecode artifact red
  fixtures.
- [x] Register `map` as a native function and add static checking for array
  element context, one-argument callbacks, return element inference, and
  generic callback rejection.
- [x] Add `map` to builtin member recognition and IR native-call lowering.
- [x] Execute callbacks in the Rust VM over a source-element snapshot and
  preserve callback/native runtime stack information.
- [x] Generate/review AST, IR, bytecode, `.cdbc`, and Rust VM outputs.
- [x] Update `README.md`, `AGENTS.md`, and `docs/roadmap.md` while leaving
  `filter` and `reduce` deferred.
- [x] Run focused and full verification, clean Python caches, and commit the
  completed slice.
