fun takesString(value: string): string {
  return value;
}

let name: string? = "Ada";
if (name == nil) {
  print "missing";
} else {
  print takesString(name);
}

let title: string? = "Dr";
if (nil != title) {
  print takesString(title);
}
