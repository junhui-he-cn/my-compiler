let numbers = [];
numbers.push(1);
let popped: number = numbers.pop();
print popped;

let strings = [];
push(strings, "a");
let first: string = strings[0];
print first;

let maybe = [1];
maybe.push(nil);
let item: number? = maybe[1];
print item;

let total = 0;
for value in numbers {
  total = total + value;
}
print total;
