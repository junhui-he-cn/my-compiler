let nullableValues: [number?] = [1, nil];
let first: number? = nullableValues.find(fun (value: number?): bool {
  return value != nil;
});
print first;

let nonNullableValues: [number] = [2];
print nonNullableValues.find(fun (value: number?): bool {
  return value != nil;
});
