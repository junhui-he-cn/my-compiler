import "./lib.cd" as lib;

fun render(value: lib.Outcome): string {
  return match value {
    lib.Outcome.Good(message: text) => text,
    lib.Outcome.Bad(code: numberValue) => str(numberValue),
  };
}

print render(lib.make());
