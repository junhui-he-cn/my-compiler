struct Counter { value: number }

impl Counter {
  fun inc(delta: number): number {
    this.value += delta;
    return this.value;
  }
}

let c: Counter = Counter { value: 1 };
print c.inc(4);
print c.value;
