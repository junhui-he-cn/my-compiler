let values = {"a": 1, "b": 2, "c": 3};
for key in values {
  print key;
  remove(values, key);
}
print values;

let numbers: map<number, string> = {1: "one", 2: "two"};
for key in numbers {
  print key;
}
